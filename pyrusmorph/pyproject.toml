[build-system]
requires = ["setuptools==65.5.0", "wheel", "pybind11>=2.10"]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
build = ["cp37-*", "cp38-*", "cp39-*", "cp310-*", "cp311-*", "cp312-*"]
manylinux-x86_64-image = "manylinux2014"
